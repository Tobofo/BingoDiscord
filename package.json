{
  "name": "bingo-discord",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --outfile=app.js"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "latest"
  },
  "devDependencies": {
    "esbuild": "latest"
  }
}
